{
  "name": "studyflow-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API for StudyFlow.",
  "scripts": {
    "build": "node --check src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "node --check src/server.js",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
